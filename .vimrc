" Vim Settings
set number				" Show line numbers by default
syntax enable			" Enable syntax highlighting
colorscheme badwolf		" Enable color scheme
set tabstop=4			" Set number of visual spaces per tab
set softtabstop=4		" Set number of spaces in tab when editing
set showcmd				" Show previous command in bottom right
set wildmenu			" Show file path complete on tab
set lazyredraw			" Only redraw when we need to
set showmatch			" Highlight matching [{()}]

" Searching
set incsearch			" Highlight as searches are coming in
set hlsearch			" Highlight
